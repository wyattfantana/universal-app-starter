# API
VITE_API_URL=http://localhost:3001

# Authentication (Better Auth - User Auth)
# Better Auth handles authentication via API at /api/auth/*
# Email/password + OAuth (Google, Facebook) configured in apps/api/.env
# No client-side API keys needed - Better Auth is server-side only

# Admin Authentication (Separate System)
# Admin uses separate session-based auth (not Better Auth)
# Admin credentials configured in apps/api/.env
# Admin login: http://localhost:5173/system/control

# Error Tracking (Sentry)
VITE_SENTRY_DSN=https://...@sentry.io/...

# Payments (Stripe)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Analytics (PostHog)
VITE_POSTHOG_KEY=phc_...
VITE_POSTHOG_HOST=https://app.posthog.com

# File Upload (Uploadthing)
VITE_UPLOADTHING_APP_ID=...
